# ===== JWT / Auth =====
# Идентификатор издателя (iss), который ожидается в токене
AUTH_ISSUER=http://localhost:8999
# JWKS endpoint для валидации подписи токена
AUTH_JWKS_URL=http://localhost:8999/.well-known/jwks.json
# Ожидаемая аудитория (aud)
AUTH_AUDIENCE=evaluation-service

# ===== HTTP Server =====
HTTP_SERVER_PORT=8080

# ===== Database (preferred granular vars) =====
DB_HOST=localhost
DB_PORT=5432
DB_USER=evaluation_user
DB_PASS=evaluation_password
DB_NAME=evaluation_db
DB_SCHEMA=evaluation

# ===== (Опционально) Единая строка подключения =====
# Если хочешь использовать одну переменную — раскомментируй и прокинь её своему рантайму.
# DB_URL=postgres://evaluation_user:evaluation_password@localhost:5432/evaluation_db?sslmode=disable&search_path=evaluation
